---
title: 2016 Faculty
navigation: Faculty
---

Below is a list of confirmed faculty members for the <strong>2016 Atlanta Photojournalism Seminar</strong>:

<% faculty = YAML.load(File.read("source/faculty/speakers/speakers.yml")) %>

<% faculty.each do |f| %>
  <div class="faculty-section">
  <h3 id="<%= f['id'] %>"><%= f['name'] %></h3>
  <% if f['url'] && f['url'] != "" %>
    <img src="<%= f['url'] %>" alt="<%= f['name'] %>" class="thumbnail faculty-image">
  <% end %>
  <% f['bio'].each do |paragraph| %>
    <p><%= paragraph %></p>
  <% end %>
  </div>
  <br>
<% end %>