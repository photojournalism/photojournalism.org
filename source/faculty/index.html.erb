---
title: Faculty
navigation: Faculty
---

<h1>2014 Faculty</h1>

Below is a list of confirmed faculty members for the <strong>2014 Atlanta Photojournalism Seminar</strong>:

<% faculty = YAML.load(File.read("source/faculty/faculty.yml")) %>
<% faculty.each do |f| %>
  <div class="faculty-section">
  <h3><%= f['name'] %></h3>
  <img src="<%= f['url'] %>" alt="<%= f['name'] %>" class="thumbnail faculty-image">
  <% f['bio'].each do |paragraph| %>
    <p><%= paragraph %></p>
  <% end %>
  </div>
<% end %>