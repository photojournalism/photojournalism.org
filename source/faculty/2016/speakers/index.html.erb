---
title: 2016 Speakers
navigation: Faculty
comments: off
---

Below is a list of confirmed speakers for the <strong>2016 Atlanta Photojournalism Seminar</strong>:

<% faculty = YAML.load(File.read("source/faculty/2016/speakers/speakers.yml")) %>

<% faculty.each do |f| %>
  <div class="faculty-section" style="float:left">
  <h3 id="<%= f['id'] %>"><%= f['name'] %></h3>
  <% if f['url'] && f['url'] != "" %>
    <img src="<%= f['url'] %>" alt="<%= f['name'] %>" class="thumbnail faculty-image">
  <% end %>
  <% f['bio'].each do |paragraph| %>
    <p><%= paragraph %></p>
  <% end %>
  </div>
<% end %>