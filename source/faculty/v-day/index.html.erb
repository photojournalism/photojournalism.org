---
title: 2014 V-Day Speakers
navigation: Faculty
comments: on
---

Below is a list of confirmed speakers for Video Day at the <strong>2014 Atlanta Photojournalism Seminar</strong>:

<% faculty = YAML.load(File.read("source/faculty/v-day/v-day.yml")) %>
<% faculty.each do |f| %>
  <div class="faculty-section">
  <h3 id="<%= f['id'] %>"><%= f['name'] %></h3>
  <img src="<%= f['url'] %>" alt="<%= f['name'] %>" class="thumbnail faculty-image">
  <% f['bio'].each do |paragraph| %>
    <p><%= paragraph %></p>
  <% end %>

  <% if defined? f['talk'] %>
    <h4>Topic: <%= f['talk']['topic'] %></h4>
    <p><%= f['talk']['description'] %></p>
  <% end %>
  </div>
<% end %>