---
title: 2018 Sponsors
navigation: Sponsors
---

<% data = YAML.load_file("source/sponsors/2018/sponsors.yml") %>

<div class='sponsors-2016'>
  <% data.each do |level, sponsors| %>
    <% if level != '' %><h2><%= level.capitalize %> Level</h2><% end %>
    <% sponsors.each do |sponsor| %>
      <a href="<%= sponsor[:homepage] %>" target="_blank">
        <img src="<%= sponsor[:image_url] %>" alt="<%= sponsor[:name] %>" height="<%= sponsor[:image_height] %>" <%= if sponsor[:dark] then "class='dark'" end %>>
      </a>
    <% end %>
  <% end %>
</div>